@model IEnumerable<GS.Models.TimeCourse>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Timestart)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Timeend)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Timestart)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Timeend)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Idtimece">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Idtimece">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Idtimece">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
